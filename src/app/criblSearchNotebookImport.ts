/**
 * App-layer re-exports for Cribl Search Notebook import (features must not import platform directly).
 */
export {
  fetchCriblSearchNotebook,
  listCriblSearchNotebooks,
} from '@platform/cribl/searchNotebooks'
